{
  "hash": "cff8d3a0cac6df783342cd113d1319b7",
  "result": {
    "engine": "knitr",
    "markdown": "---\ntitle: \"minijoin\"\n---\n\n\n\n\n\nThe purpose of **minijoin** is to provide a zero-dependency slice of\n[**dplyr**](https://dplyr.tidyverse.org/)-style syntax for mutating\njoins, all built atop base R's `merge()`. The [README](https://github.com/djnavarro/minis/tree/main/minijoin)\nprovides the full argument reference; this page compares all four join\nfunctions side by side on two small toy data frames with a partial\noverlap, then covers the `by` argument in more detail.\n\n## Functions\n\nThe [**minijoin.R**](https://github.com/djnavarro/minis/blob/main/minijoin/minijoin.R)\nscript supplies four core functions:\n\n- `.join_inner_join(x, y, by, ...)` is used to keep only rows with a\n  match in both `x` and `y`.\n- `.join_left_join(x, y, by, ...)` is used to keep all rows of `x`,\n  filling in `NA` for `y`'s columns where there's no match.\n- `.join_right_join(x, y, by, ...)` is used to keep all rows of `y`,\n  filling in `NA` for `x`'s columns where there's no match.\n- `.join_full_join(x, y, by, ...)` is used to keep all rows of both\n  `x` and `y`, filling in `NA` on whichever side has no match.\n\nThere is also one internal helper:\n\n- `.join_worker()` is the shared implementation that all four join\n  functions delegate to, after fixing the right combination of\n  `merge()`'s `all.x`/`all.y` arguments; it also restores `x`'s\n  original row order, which `merge()` doesn't guarantee on its own.\n\n## Minimal data frames\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbands <- data.frame(\n  band = c(\"Beatles\", \"Rolling Stones\", \"Kinks\"),\n  founded = c(1960, 1962, 1963)\n)\nalbums <- data.frame(\n  band = c(\"Beatles\", \"Kinks\", \"Queen\"),\n  album = c(\"Abbey Road\", \"Arthur\", \"A Night at the Opera\")\n)\nbands\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n            band founded\n1        Beatles    1960\n2 Rolling Stones    1962\n3          Kinks    1963\n```\n\n\n:::\n\n```{.r .cell-code}\nalbums\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n     band                album\n1 Beatles           Abbey Road\n2   Kinks               Arthur\n3   Queen A Night at the Opera\n```\n\n\n:::\n:::\n\n\n\nHere we have `bands` and `albums` as the data sets, sharing\n`\"Beatles\"` and `\"Kinks\"`. The `\"Rolling Stones\"` exist only in\n`bands`, but `\"Queen\"` exists only in `albums`. The four join types\ndiffer only in which of those non-matching rows they keep.\n\n## Inner join: only rows matching in both\n\nNeither `\"Rolling Stones\"` nor `\"Queen\"` appears -- an inner join is the most restrictive of the four, keeping only rows with a counterpart on the other side:\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\n.join_inner_join(bands, albums, by = \"band\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n     band founded      album\n1 Beatles    1960 Abbey Road\n2   Kinks    1963     Arthur\n```\n\n\n:::\n:::\n\n\n\n## Left join: all rows of `x` (`bands`)\n\nThe `\"Rolling Stones\"` row is kept, with `NA` for `album`, since it has\nno match in `albums`. This is the join to reach for when `x` is your\n\"main\" table and you're enriching it with optional data from `y`:\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\n.join_left_join(bands, albums, by = \"band\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n            band founded      album\n1        Beatles    1960 Abbey Road\n2 Rolling Stones    1962       <NA>\n3          Kinks    1963     Arthur\n```\n\n\n:::\n:::\n\n\n\n## Right join: all rows of `y` (`albums`)\n\nThe `\"Queen\"` row is kept, with `NA` for `founded`. A right join is\nreally just a left join with the arguments swapped --\n`.join_right_join(x, y, ...)` behaves like `.join_left_join(y, x, ...)`,\nwith columns in `x`'s original order. Note the row order below:\nmatched rows still come first, in `bands`' original order, with the\nunmatched-in-`x` row (`\"Queen\"`) appended at the end -- `minijoin`\nalways restores `x`'s original row order for matched rows, rather than\n`y`'s:\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\n.join_right_join(bands, albums, by = \"band\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n     band founded                album\n1 Beatles    1960           Abbey Road\n2   Kinks    1963               Arthur\n3   Queen      NA A Night at the Opera\n```\n\n\n:::\n:::\n\n\n\n## Full join: all rows of both\n\nBoth `\"Rolling Stones\"` and `\"Queen\"` are kept, each with an `NA` in the column they don't have a match for -- a full join never drops a row from either side, so it's the join to reach for when you want to see everything and let missing matches show up as `NA` rather than disappearing silently:\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\n.join_full_join(bands, albums, by = \"band\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n            band founded                album\n1        Beatles    1960           Abbey Road\n2 Rolling Stones    1962                 <NA>\n3          Kinks    1963               Arthur\n4          Queen      NA A Night at the Opera\n```\n\n\n:::\n:::\n\n\n\n## `by` is always explicit\n\nUnlike `dplyr`, there's no auto-detection of shared column names -- `by` must always be supplied. This is a deliberate simplification, not an oversight, to avoid a class of silent bugs from an unintended shared column name (two data frames that happen to both have an `id` column with unrelated meanings, for instance).\n\nWhen the join column has different names on each side, `by` takes a named character vector instead of a single string, `c(\"x_name\" = \"y_name\")` -- mirroring `dplyr`'s own syntax for the same situation:\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nalbums_renamed <- albums\nnames(albums_renamed)[1] <- \"artist\"\nalbums_renamed\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n   artist                album\n1 Beatles           Abbey Road\n2   Kinks               Arthur\n3   Queen A Night at the Opera\n```\n\n\n:::\n\n```{.r .cell-code}\n.join_inner_join(bands, albums_renamed, by = c(\"band\" = \"artist\"))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n     band founded      album\n1 Beatles    1960 Abbey Road\n2   Kinks    1963     Arthur\n```\n\n\n:::\n:::\n\n\n\nNote that the output keeps `x`'s column name (`band`), not `y`'s (`artist`) -- the join collapses the two into a single column, using whichever name `x` had.\n",
    "supporting": [],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {},
    "engineDependencies": {},
    "preserve": {},
    "postProcess": true
  }
}